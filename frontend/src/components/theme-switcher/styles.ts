import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  iconThemeBlock: {
    paddingTop: '5px',
  },

  iconNotificationsBlock: {
    paddingRight: '37px',
    paddingTop: '5px',
  },
  themeIcon: {
    marginRight: '45px',
  },
});
